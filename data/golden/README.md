# Golden outputs

Generated by `python scripts/run_pipeline.py`. These files are committed so a reviewer can inspect outputs without rerunning. Rebuild anytime with the same command.

Expected files:

- `golden_account_month.parquet` — grain account_id × month_ist
- `fact_payment.parquet` — unique payment_id
- `fact_interaction.parquet`
- `fact_payment_attribution.parquet`
- `dim_account.parquet`, `dim_agent.parquet`, `dim_borrower.parquet`, `dim_campaign.parquet`, `dim_vendor.parquet`
- `metrics_monthly.parquet`, `metrics_drivers_snapshot.parquet`, `metrics_vendor.parquet`, `metrics_channel_attribution.parquet`
- `claim_reconciliation.json`, `counterfactual.json`, `cleaning_impact.json`
